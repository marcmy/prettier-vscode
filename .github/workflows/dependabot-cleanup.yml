name: Dependabot cleanup

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/dependabot-cleanup.yml

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out the PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Update vulnerable dependencies
        shell: bash
        run: |
          set -euo pipefail
          npm pkg set 'overrides.diff=8.0.3' 'overrides.serialize-javascript=7.0.5'
          npm update --package-lock-only
          npm audit fix --package-lock-only
          npm audit --audit-level=low

      - name: Verify dependency tree
        shell: bash
        run: |
          set -euo pipefail
          npm ci
          npm run check-types
          npm run lint
          npm run compile

      - name: Validate package-only diff
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          unexpected="$(git diff --name-only | grep -vE '^(package.json|package-lock.json)$' || true)"
          if [[ -n "$unexpected" ]]; then
            echo "Unexpected changed files:"
            echo "$unexpected"
            exit 1
          fi
          git diff --stat

      - name: Commit verified dependency update
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json
          git commit -m "chore: update vulnerable dependencies"
          git push origin HEAD:${{ github.head_ref }}
